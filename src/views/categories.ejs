<%- include('partials/header') %>
    <main>
        <h1>Categories</h1>
        <ul>
            <% categories.forEach(category => { %>
                <li><a href="/category/<%= category.category_id %>"><%= category.name %></a></li>
            <% }); %>
        </ul>
        <a href="/new-category" class="btn-primary">Add a new category</a>
    </main>
<%- include('partials/footer') %>