<%- include('partials/header') %>
    <main>
        <h1>Categories</h1>
        <ul>
            <% categories.forEach(category => { %>
                <li><%= category.name %></li>
            <% }); %>
        </ul>
    </main>
<%- include('partials/footer') %>